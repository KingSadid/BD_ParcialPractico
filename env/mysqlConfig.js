const heritageConfig = {
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'db_heritage',
    port: 3306,
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
};

module.exports = heritageConfig;